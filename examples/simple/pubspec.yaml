name: simple
description: A new Flutter project.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  managed: 1.1.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^2.0.0
  build_runner: ^2.4.8
  managed_gen:
    path: ../../../managed_gen

dependency_overrides:
  managed:
    path: ../../../managed

flutter:

  uses-material-design: true
